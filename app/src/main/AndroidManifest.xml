<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="edu.whitworth.test1">

<uses-permission android:name="android.permission.INTERNET" />

<application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name"
    android:supportsRtl="true" android:theme="@style/AppTheme">
    android:debuggable = "true"
    <activity android:name=".MainActivity" android:label="Star Realms">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".Profile" android:label="Profile" />
    <activity android:name=".learnPlay" android:label="Learn to Play" />
    <activity android:name=".VsfriendActivity" android:label="Play vs Friends" />
    <activity android:name=".Play" android:label="@string/title_activity_play" />
    <activity android:name=".Winning" android:label="@string/title_activity_winning" />
    <activity android:name=".Losing" android:label="@string/title_activity_losing"></activity>
</application>

</manifest>
